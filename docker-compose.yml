services:
  frontend:
    image: ghcr.io/asolmaz55/group6-frontend:latest
    build:
      context: ./frontend
    ports:
      - "8080:8080"
    environment:
      BACKEND_DNS: localhost
      BACKEND_PORT: "9000"